generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Account {
  id                String  @id @default(dbgenerated("gen_random_uuid()"))
  userId            String
  user              users   @relation(fields: [userId], references: [id], onDelete: Cascade)
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        BigInt?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  @@unique([provider, providerAccountId], map: "provider_unique_id")
  @@index([userId])
}

model Session {
  id           String   @id @default(dbgenerated("gen_random_uuid()"))
  sessionToken String   @unique
  userId       String
  expires      DateTime @db.Timestamptz(6)
  user         users    @relation(fields: [userId], references: [id])

  @@index([userId])
}

model VerificationToken {
  identifier String
  token      String
  expires    DateTime @db.Timestamptz(6)

  @@unique([identifier, token], map: "token_identifier_unique")
}

model auctions {
  id                                 String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  lead_id                            String         @db.Uuid
  minimum_bid                        Decimal        @db.Decimal(10, 2)
  status                             String         @default("open") @db.VarChar(20)
  type                               auction_type_enum @default(single)
  batch_auction_id                   String?        @unique @db.Uuid
  winning_bid_id                     String?        @unique @db.Uuid
  expired_at                         DateTime       @db.Timestamptz(6)
  created_at                         DateTime?      @default(now()) @db.Timestamptz(6)
  leads                              leads          @relation(fields: [lead_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  batch_auction                      batch_auctions? @relation("BatchAuctionAuction", fields: [batch_auction_id], references: [id], onDelete: SetNull)
  bids_auctions_winning_bid_idTobids bids?          @relation("auctions_winning_bid_idTobids", fields: [winning_bid_id], references: [id], onUpdate: NoAction, map: "fk_winning_bid")
  bids_bids_auction_idToauctions     bids[]         @relation("bids_auction_idToauctions")
  credit_holds                       credit_holds[]

  @@index([lead_id])
  @@index([status, expired_at], map: "idx_auctions_status_expired_at")
}

model bids {
  id                                     String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  auction_id                             String         @db.Uuid
  user_id                                String
  amount                                 Decimal        @db.Decimal(10, 2)
  created_at                             DateTime?      @default(now()) @db.Timestamptz(6)
  auctions_auctions_winning_bid_idTobids auctions?      @relation("auctions_winning_bid_idTobids")
  auctions_bids_auction_idToauctions     auctions       @relation("bids_auction_idToauctions", fields: [auction_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                                  users          @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  credit_holds                           credit_holds[]

  @@index([auction_id])
  @@index([user_id])
  @@index([auction_id, created_at(sort: Desc)], map: "idx_bids_auction_id_created_at")
}

model credit_transactions {
  id                     BigInt           @id @default(autoincrement())
  asaas_payment_id       String?          @unique
  user_id                String
  amount_paid            Decimal          @db.Decimal(10, 2)
  credits_purchased      Decimal          @db.Decimal(10, 2)
  metadata               Json?
  status                 String           @default("completed") @db.VarChar(50)
  created_at             DateTime?        @default(now()) @db.Timestamptz(6)
  infinitepay_payment_id String?          @unique
  source                 credit_source_enum @default(monetary)
  users                  users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  ledger_entries         ledger_entries[]

  @@index([user_id])
}

model leads {
  id                   String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  company_name         String     @db.VarChar(255)
  contact_name         String     @db.VarChar(255)
  phone                String     @db.VarChar(50)
  email                String     @db.VarChar(255)
  revenue              String
  marketing_investment String
  segment              String?    @db.VarChar(100)
  minimum_value        Decimal?   @db.Decimal(15, 2)
  status               String?    @db.VarChar(50)
  channel              String?    @db.VarChar(100)
  owner_id             String?
  created_at           DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?  @default(now()) @db.Timestamptz(6)
  cnpj                 String?    @db.VarChar(32)
  city                 String?    @db.VarChar(255)
  state                String?    @db.VarChar(100)
  document_url         String?    @db.VarChar(2048)
  contract_url         String?    @db.VarChar(2048)
  contract_value       Float?
  call_url             String?
  contract_time        String?
  contract_installments Int?
  briefing_url         String?
  batched_at           DateTime?  @db.Timestamptz(6)
  batch_auction_id     String?    @db.Uuid
  batch_result         String?    @db.VarChar(50)
  auctions             auctions[]
  users                users?     @relation(fields: [owner_id], references: [id], onUpdate: NoAction)
  current_batch        batch_auctions?        @relation("LeadCurrentBatch", fields: [batch_auction_id], references: [id], onDelete: SetNull)
  batch_history        batch_auction_leads[]

  @@index([owner_id])
  @@index([status])
  @@index([owner_id], map: "idx_leads_owner_id")
  @@index([batch_auction_id], map: "leads_batch_auction_id_idx")
}

model ledger_entries {
  id                  BigInt              @id @default(autoincrement())
  transaction_id      BigInt
  user_id             String
  account_type        account_type_enum
  amount              Decimal             @db.Decimal(10, 2)
  created_at          DateTime?           @default(now()) @db.Timestamptz(6)
  credit_source       credit_source_enum?
  credit_transactions credit_transactions @relation(fields: [transaction_id], references: [id], onUpdate: NoAction)
  users               users               @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@index([transaction_id])
  @@index([user_id])
}

model checkout_sessions {
  id                   BigInt    @id @default(autoincrement())
  asaas_checkout_id    String?   @unique
  internal_checkout_id String
  user_id              String
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  users                users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "checkout_sessions_user_fk")

  @@index([user_id], map: "idx_checkout_sessions_user_id")
}

model processed_webhooks {
  event_key  String              @id
  status     webhook_status_enum @default(queued)
  created_at DateTime?           @default(now()) @db.Timestamptz(6)
}

model users {
  id                  String                @id
  credit_balance      Decimal               @default(0.00) @db.Decimal(10, 2)
  created_at          DateTime              @default(now()) @db.Timestamptz(6)
  updated_at          DateTime              @default(now()) @db.Timestamptz(6)
  name                String?
  email               String?
  email_verified      DateTime?             @db.Timestamptz(6)
  avatar_url          String?
  accepted_terms_at   DateTime?             @db.Timestamptz(6)
  role                user_role             @default(user)
  bids                bids[]
  checkout_sessions   checkout_sessions[]
  credit_transactions credit_transactions[]
  leads               leads[]
  ledger_entries      ledger_entries[]
  Session             Session[]
  Account             Account[]

  @@map("users")
}

model User {
  id            String    @id
  name          String?
  email         String?   @unique
  emailVerified DateTime? @db.Timestamptz(6)
  image         String?
}

model credit_holds {
  id         BigInt                  @id @default(autoincrement())
  user_id    String
  auction_id String                  @db.Uuid
  amount     Decimal                 @db.Decimal(10, 2)
  status     credit_hold_status_enum @default(active)
  created_at DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at DateTime?               @default(now()) @db.Timestamptz(6)
  bid_id     String?                 @db.Uuid
  auctions   auctions                @relation(fields: [auction_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  bids       bids?                   @relation(fields: [bid_id], references: [id], onUpdate: NoAction)

  @@unique([user_id, auction_id], map: "credit_holds_user_auction_unique")
  @@index([auction_id], map: "idx_credit_holds_auction_id")
  @@index([user_id], map: "idx_credit_holds_user_id")
  @@index([status], map: "idx_credit_holds_status")
}

model batch_auction_settings {
  id                   String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  low_frozen_threshold Int
  auto_trigger_enabled Boolean   @default(true)
  lead_unit_price      Decimal   @default(225.00) @db.Decimal(10, 2)
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_by           String?
}

model batch_auctions {
  id             String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  total_leads    Int
  lead_unit_price Decimal                   @db.Decimal(10, 2)
  minimum_bid    Decimal                    @db.Decimal(10, 2)
  status         batch_auction_status_enum  @default(open)
  result         batch_auction_result_enum  @default(pending)
  trigger_reason batch_auction_trigger_enum @default(auto)
  created_at     DateTime?                  @default(now()) @db.Timestamptz(6)
  expired_at     DateTime?                  @db.Timestamptz(6)
  closed_at      DateTime?                  @db.Timestamptz(6)
  metadata       Json?

  entries         batch_auction_leads[]
  auction         auctions?                 @relation("BatchAuctionAuction")
  current_assignments leads[]               @relation("LeadCurrentBatch")

  @@index([status], map: "batch_auctions_status_idx")
  @@index([result], map: "batch_auctions_result_idx")
}

model batch_auction_leads {
  batch_auction_id   String    @db.Uuid
  lead_id            String    @db.Uuid
  included_at        DateTime? @default(now()) @db.Timestamptz(6)
  status_before_batch String
  final_status       String?
  sold_at            DateTime? @db.Timestamptz(6)

  batch batch_auctions @relation(fields: [batch_auction_id], references: [id], onDelete: Cascade)
  lead  leads          @relation(fields: [lead_id], references: [id], onDelete: Cascade)

  @@id([batch_auction_id, lead_id])
  @@unique([lead_id], map: "batch_auction_leads_lead_id_key")
  @@index([lead_id], map: "batch_auction_leads_lead_id_idx")
}

model request_keys {
  key        String    @id
  created_at DateTime? @default(now()) @db.Timestamptz(6)

  @@map("request_keys")
}

model google_chat_webhook_logs {
  id            BigInt    @id @default(autoincrement())
  auction_id    String?   @db.Uuid
  payload_text  String?
  status        Int?
  response_text String?
  error_text    String?
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model v_lead_count {
  nextval BigInt?

  @@ignore
}

enum account_type_enum {
  USER_CREDITS
  PLATFORM_REVENUE
}

enum credit_source_enum {
  monetary
  reward
  adjustment
}

enum webhook_status_enum {
  queued
  processed
  failed
  ignored
}

enum credit_hold_status_enum {
  active
  released
  consumed
}

enum user_role {
  user
  admin
}

enum auction_type_enum {
  single
  batch
}

enum batch_auction_status_enum {
  open
  running
  completed
  cancelled
}

enum batch_auction_result_enum {
  pending
  sold
  expired
}

enum batch_auction_trigger_enum {
  auto
  manual
}
